<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="HandheldFriendly" content="True" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <title>dEv-Commerce</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-sky-400">
  <header class="px-4 py-2 bg-sky-900 text-sky-200 flex justify-between">
    <a href="/" class="w-16">
      <image src="/images/dEv-Commerce_logo.png" alt="dEv Commerce application's logo"></image>
    </a>
    {{!-- <div>
      <form id="searchForm">
        <label for="search-category">Select a Category to Search from:</label>
        <select name="search-category" id="search-category" class="text-sky-900 w-28">
          <option value="1">Hardware</option>
          <option value="2">Software</option>
        </select>
        <button id="searchFormBtn" type="submit" class="ml-1 px-2 bg-stone-400 text-stone-700 rounded hover:bg-sky-200 hover:text-sky-900">Search</button>
      </form>
      <form id="tagSearch" class="mt-1">
        <label>Select A Tag to Search From:</label>
        <select name="search-tag" id="search-tag" class="text-sky-900 w-28">
          {{#each tags}}
          <option value="{{id}}">{{name}}</option>
          {{/each}}
        </select>
        <button id="searchTagBtn" type="submit" class="ml-1 px-2 bg-stone-400 text-stone-700 rounded hover:bg-sky-200 hover:text-sky-900">Search</button>
      </form>
    </div> --}}

    <nav class="py-3">
      <a href="/profile" class="mx-3">My Profile</a>
      {{#if loggedIn}}
      <button id="logoutBtn" class="mx-3">Logout</button>
      {{else}}
      <a href="/login" class="mx-3">Login</a>
      {{/if}}
    </nav>
  </header>

  <main>
    {{{ body }}}
  </main>

  {{!-- <footer class="p-2 bg-sky-900 text-sky-200 fixed bottom-0 w-screen"> --}}
  <footer class="p-2 bg-sky-900 text-sky-200">
    <div>Products sold by web developers, for web developers. copyright &copy; 2022</div>
  </footer>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
  <script src="/javascript/search.js"></script>
</body>
